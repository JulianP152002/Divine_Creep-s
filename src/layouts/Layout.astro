<!doctype html>
<html lang="es">
	<head>
		<!-- Primary Meta Tags -->
	<title>Divine Creep's</title>
	<meta name="title" content="Divine Creep's" />
	<meta name="description" content="Somos un negocio de las famosas y deliciosas creepas de dulce y saladas, estamos ubicados en la ciudad de Tunja, Boyaca" />

	<!-- Open Graph / Facebook -->
	<meta property="og:type" content="website" />
	<meta property="og:url" content="https://metatags.io/" />
	<meta property="og:title" content="Divine Creep's" />
	<meta property="og:description" content="Somos un negocio de las famosas y deliciosas creepas de dulce y saladas, estamos ubicados en la ciudad de Tunja, Boyaca" />
	<meta property="og:image" content="https://metatags.io/images/meta-tags.png" />

	<!-- Twitter -->
	<meta property="twitter:card" content="summary_large_image" />
	<meta property="twitter:url" content="https://www.instagram.com/divine_creeps/" />
	<meta property="twitter:title" content="Divine Creep's" />
	<meta property="twitter:description" content="Somos un negocio de las famosas y deliciosas creepas de dulce y saladas, estamos ubicados en la ciudad de Tunja, Boyaca" />
	<meta property="twitter:image" content="https://metatags.io/images/meta-tags.png" />

<!-- Meta Tags Generated with https://metatags.io -->
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/logo.png" />
		<meta name="generator" content={Astro.generator} />
		
		<title>Divine Creep's</title>
		
	</head>
	<body>
		<slot />
		<script  >
			const imgs = document.querySelectorAll(".container");
			console.log(imgs);
			function calback(entries:any ): void {
				entries.forEach((entry:any) => {
					const image= entry.target.querySelector('img')
					image.classList.toggle('unset', entry.isIntersecting);
				});
			}
			const options = {
  				root: null,
  				rootMargin: "10px",
  				threshold: 0,
			};

			const observer = new IntersectionObserver(calback, options);

			imgs.forEach((img) => {
  				observer.observe(img);
			});

		</script>

	</body>


</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
	   .hero-reverse {
  flex-direction: row-reverse;
}
    
[data-animation="fade"]{
    opacity: 0;
    transform: translate(100%);
    transition: .6s ease-in-out .3s;

    --opacity: 1;
    --transform: translate(0);
}

[data-animation="show"]{
    opacity: 0;
    transform: scale(0);
    transition: .6s ease-in-out .3s;

    --opacity: 1;
    --transform: scale(1);
}

[data-animation="up"]{
    opacity: 0;
    transform: translateY(100%);
    transition: .6s ease-in-out .3s;

    --opacity: 1;
    --transform: translateY(0);
}


.unset{
    opacity: var(--opacity);
    transform: var(--transform);

}
</style>
